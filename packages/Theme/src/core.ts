import { css } from "styled-components";

export const Theme = css`
    :root {
        --table-body-background: var(--bx--table-body-backgorund);
        --table-body-background: var(--bx--table-body-backgorund);
        --table-row-border-bottom: var(--bx--table-row-border-bottom);
        --table-row-min-height: var(--bx--table-row-min-height);
    }
`;
