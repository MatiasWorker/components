import "@bxreact/theme";
import { ReactNode } from "react";
import "./index.css";
export declare function Header({ title, children, required, }: {
    title: ReactNode;
    required?: boolean;
    children?: ReactNode;
}): JSX.Element;
